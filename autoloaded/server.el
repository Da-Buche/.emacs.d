;; =============================================================================================================
;; Start emacs server
;; =============================================================================================================
;(defvar server-auth-key "NLjB36$$|e3reA?%@Q{W]80}M'4&~rEPuES1V%3%2{}aYLO}:fw>,>Q}NB3XLGCN")
;(defvar server-use-tcp t)
;(defvar server-name "/home/bucheta1/.emacs.d/.server")
;(defvar server-socket-dir
;  (and (featurep 'make-network-process '(:family local))
;    ;(format "%s/emacs%d" (or (getenv "TMPDIR") "/tmp") (user-uid))
;    ;(format "%s/emacs%d" "/tmp" (user-uid))
;    "/home/bucheta1/.emacs.d/server/.emacs.socket")
;  "The directory in which to place the server socket.
;  If local sockets are not supported, this is nil.")
(server-start)




































